name: Automation Tests

on:
  workflow_dispatch:
  schedule:
    - cron: ‘53 11 * * *’
    - cron: ‘54 11 * * *’

jobs:
  ui-chrome-smoke-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Smoke Test
        if: github.event.schedule == '53 11 * * *'
        run: |
          echo "${{ github.event.schedule }}"
        
  ui-chrome-sanity-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Sanity Test
        if: github.event.schedule == '54 11 * * *'
        run: |
          echo "hii ${{ github.event.schedule }}"
        
