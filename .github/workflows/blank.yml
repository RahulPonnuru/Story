name: Automation Tests

on:
  workflow_dispatch:
  schedule:
    - cron: '15 9 * * *'
    - cron: '17 9 * * *'

jobs:
  ui-chrome-smoke-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Smoke Test
        if: github.event.schedule == '17 9 * * *'
        run: |
          echo "${{ github.event.schedule }}"

      - name: Sanity Test
        if: github.event.schedule == '15 9 * * *'
        run: |
          echo "hii ${{ github.event.schedule }}"
       
      - name: anity Test
        run: |
          echo "hii ${{ github.event.schedule }}"

      - name: Print UTC Time
        run: |
          echo "Current UTC Time: $(date -u)"
        
      
